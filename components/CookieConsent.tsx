"use client";
import { useEffect, useState } from "react";
export default function CookieConsent() { const [visible, setVisible] = useState(false); useEffect(() => { setVisible(localStorage.getItem("lyjy_cookie_consent") !== "accepted"); }, []); if (!visible) return null; const accept = () => { localStorage.setItem("lyjy_cookie_consent", "accepted"); setVisible(false); }; return <div className="fixed bottom-0 left-0 right-0 z-[70] border-t border-[#C4A77D]/50 bg-stone-950 p-4 text-xs text-stone-200"><div className="mx-auto flex max-w-5xl flex-col gap-3 md:flex-row md:items-center md:justify-between"><p>LYJY utilise des cookies nécessaires au panier et à la connexion, ainsi que des cookies de mesure pour améliorer le site.</p><div className="flex gap-2"><button onClick={accept} className="bg-[#C4A77D] px-4 py-2 text-black uppercase">Accepter</button><button onClick={accept} className="border border-stone-600 px-4 py-2 uppercase">Continuer sans statistiques</button></div></div></div>; }
